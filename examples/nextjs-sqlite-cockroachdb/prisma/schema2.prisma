// generator letsync {
//   provider               = "letsync"
//   optimistic_column_name = "$optimistic"
// }

generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["driverAdapters"] // prismaSchemaFolder
  output          = "../node_modules/@prisma/client/DATABASE_NAME_2"
}

// datasource db {
//   type     = "local"
//   provider = "postgresql"
//   url      = env("DATABASE_URL")
// }

model User {
  id           String @id @default(cuid())
  name         String
  email        String @unique
  password     String
  /// server-only
  passwordHash String
  /// client-only
  theme        String
}
